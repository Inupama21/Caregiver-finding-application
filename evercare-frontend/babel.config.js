// Restored Babel configuration for Expo project
// This enables JSX, TypeScript, and required transforms used by expo-router.
module.exports = function (api) {
  api.cache(true);
  return {
    presets: [
      "babel-preset-expo",
    ],
    plugins: [
    
    ],
  };
};
